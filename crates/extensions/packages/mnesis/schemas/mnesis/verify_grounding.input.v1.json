{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Mnesis verify_grounding arguments",
  "description": "Published by the Mnesis MCP lane at runtime and captured verbatim; the engine remains authoritative for validation.",
  "type": "object",
  "properties": {
    "response": {
      "type": "string",
      "description": "The LLM response text to verify"
    },
    "query": {
      "type": "string",
      "description": "The original search query"
    }
  },
  "required": [
    "response",
    "query"
  ]
}
