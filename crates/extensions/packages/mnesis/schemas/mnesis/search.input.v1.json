{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Mnesis search arguments",
  "description": "Published by the Mnesis MCP lane at runtime and captured verbatim; the engine remains authoritative for validation.",
  "type": "object",
  "properties": {
    "limit": {
      "default": 5,
      "description": "Maximum number of authorized results to return",
      "type": "integer",
      "minimum": 1,
      "maximum": 20
    },
    "query": {
      "type": "string",
      "minLength": 1,
      "maxLength": 16384,
      "description": "Topic, claim, decision, or context request to retrieve"
    },
    "source": {
      "description": "Optional exact source identifier",
      "type": "string",
      "minLength": 1,
      "maxLength": 256
    }
  },
  "required": [
    "query"
  ]
}
