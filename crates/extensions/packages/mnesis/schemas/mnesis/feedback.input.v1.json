{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Mnesis feedback arguments",
  "description": "Published by the Mnesis MCP lane at runtime and captured verbatim; the engine remains authoritative for validation.",
  "type": "object",
  "properties": {
    "queryId": {
      "type": "integer",
      "exclusiveMinimum": 0,
      "maximum": 9007199254740991,
      "description": "The feedback ID returned by search_knowledge"
    },
    "wasHelpful": {
      "type": "boolean",
      "description": "Whether the search results were helpful"
    },
    "comment": {
      "description": "Optional comment about why results were/weren't helpful",
      "type": "string",
      "maxLength": 2000
    }
  },
  "required": [
    "queryId",
    "wasHelpful"
  ]
}
