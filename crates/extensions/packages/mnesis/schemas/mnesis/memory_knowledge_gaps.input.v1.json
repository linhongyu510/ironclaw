{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Mnesis memory_knowledge_gaps arguments",
  "description": "Published by the Mnesis MCP lane at runtime and captured verbatim; the engine remains authoritative for validation.",
  "type": "object",
  "properties": {
    "action": {
      "type": "string",
      "enum": [
        "detect",
        "fill",
        "fill_one",
        "status"
      ],
      "description": "detect = scan for new gaps, fill = detect + fill all, fill_one = fill specific gap, status = show queue"
    },
    "gap_id": {
      "type": "string",
      "description": "Gap ID for fill_one action"
    }
  },
  "required": [
    "action"
  ],
  "additionalProperties": false
}
