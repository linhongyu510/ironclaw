{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Mnesis training arguments",
  "description": "Published by the Mnesis MCP lane at runtime and captured verbatim; the engine remains authoritative for validation.",
  "type": "object",
  "properties": {
    "action": {
      "default": "stats",
      "description": "export: generate triplets, stats: check existing",
      "type": "string",
      "enum": [
        "export",
        "stats"
      ]
    }
  }
}
