{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Mnesis challenge_retrieval arguments",
  "description": "Published by the Mnesis MCP lane at runtime and captured verbatim; the engine remains authoritative for validation.",
  "type": "object",
  "properties": {
    "challenge": {
      "type": "string",
      "minLength": 1,
      "maxLength": 32768,
      "description": "Canonical retrieval challenge encoded as unpadded base64url"
    }
  },
  "required": [
    "challenge"
  ]
}
