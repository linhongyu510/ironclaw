{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Mnesis routing arguments",
  "description": "Published by the Mnesis MCP lane at runtime and captured verbatim; the engine remains authoritative for validation.",
  "type": "object",
  "properties": {
    "days": {
      "default": 1,
      "description": "Analyze decisions from last N days (default: 1, max depends on log capacity)",
      "type": "number"
    }
  }
}
