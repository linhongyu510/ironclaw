{BUDGET_STATUS} These file deliverable(s) still do not exist:

{MISSING_PATHS}

Writing them is the only thing left to do. Make the very next action a write of each file, straight from what is already in this conversation — no further research, no further reading, no further tool calls of any other kind.

Partial is better than absent: if a section is incomplete, write it with an explicit note saying so. Do not summarize the work in chat instead of writing the file.
